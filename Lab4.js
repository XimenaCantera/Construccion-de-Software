let num = prompt("Dame un número",0);

const arreglo = [1];

function cuadrado(a, ){
        let a = parseInt(num);
        let result = a * a;
        alert(result);
        arreglo.push(result);
        for (let elemento in arreglo){
            console.log(arreglo);
        }
    }
